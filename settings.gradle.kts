plugins {
    // Auto-download the JDK declared in the Java toolchain (see build.gradle.kts)
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "silpo-restock-ai"
