# Deliberately minimal. The scaffold once set archRule.failOnEmptyShould=false because the domain packages were
# empty; they are not any more, and a rule matching nothing now means a rule that stopped being enforced — which
# should fail rather than pass quietly. Nothing here overrides ArchUnit's defaults.
