# This is a starter template: the domain packages are still empty, so naming/layering rules would otherwise
# fail with "rule evaluated against an empty set of classes". Allow empty matches until real code lands.
archRule.failOnEmptyShould=false
