package com.silporestockai.model;

/** Whether a partner promotion (task 46) is currently eligible to be surfaced. Persisted by name. */
public enum PartnerPromotionStatus {
    ACTIVE,
    PAUSED,
    EXPIRED
}
